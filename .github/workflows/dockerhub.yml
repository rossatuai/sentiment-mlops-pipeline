name: DockerHub Push

on:
  # Runs after the MLOps Pipeline workflow finishes
  workflow_run:
    workflows: ["MLOps Pipeline"]
    types:
      - completed

jobs:
  docker:

    # Only runs if pipeline succeeded on main branch
    # Also prevents workflow loops from GitHub Actions
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.actor.login != 'github-actions'

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:

    # Gets latest repository files
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    # Logs into DockerHub using stored secrets
    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Builds Docker image with unique run number tag
    - name: Build Docker Image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/sentiment-api:${{ github.run_number }} .

    # Pushes Docker image to DockerHub
    - name: Push Docker Image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/sentiment-api:${{ github.run_number }}

    # Updates Kubernetes deployment file with new image version
    - name: Update Kubernetes Manifest
      run: |
        sed -i "s|image: .*|image: ${{ secrets.DOCKER_USERNAME }}/sentiment-api:${{ github.run_number }}|" k8s/deployment.yaml

    # Saves updated deployment file back to repository
    - name: Commit updated manifest
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"

        git add k8s/deployment.yaml

        git commit -m "Update image tag to ${{ github.run_number }}" || echo "No changes"

        git push